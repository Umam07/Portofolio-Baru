---
import { portfolioData } from '../data/portfolioData';

const { experience } = portfolioData;
---

<section id="experience" class="py-20 md:py-28 px-4 sm:px-6 lg:px-8 max-w-6xl mx-auto">
  <!-- Section Header -->
  <div class="pb-8 border-b border-[#e6dfd8]">
    <h2 class="font-display text-3xl sm:text-4xl text-[#141413] tracking-tight">
      Experience & Career Track
    </h2>
    <p class="mt-2 text-sm sm:text-base text-[#6c6a64] max-w-[50ch]">
      Product squads, design systems, and frontend engineering roles I have contributed to.
    </p>
  </div>

  <!-- Timeline Cards List -->
  <div class="mt-10 space-y-6">
    {experience.map((item) => (
      <div class="experience-card group p-6 sm:p-8 rounded-xl bg-[#efe9de] border border-[#e6dfd8] transition-all duration-200 hover:border-[#cc785c]">
        <div class="flex flex-col md:flex-row md:items-start justify-between gap-4 pb-4 border-b border-[#e6dfd8]">
          <div>
            <div class="flex items-center gap-3">
              <h3 class="font-display text-2xl sm:text-3xl text-[#141413] group-hover:text-[#cc785c] transition-colors">
                {item.role}
              </h3>
              <span class="px-2 py-0.5 rounded-full text-[11px] font-mono bg-[#faf9f5] text-[#3d3d3a] border border-[#e6dfd8]">
                {item.type}
              </span>
            </div>
            
            <p class="mt-1 text-sm font-medium text-[#3d3d3a]">
              {item.company} <span class="text-[#8e8b82] font-normal">· {item.location}</span>
            </p>
          </div>

          <span class="inline-block px-3 py-1 rounded bg-[#faf9f5] border border-[#e6dfd8] text-xs font-mono text-[#6c6a64]">
            {item.period}
          </span>
        </div>

        <p class="mt-4 text-sm text-[#252523] leading-relaxed">
          {item.description}
        </p>

        <!-- Highlights -->
        <ul class="mt-4 space-y-2">
          {item.highlights.map((highlight) => (
            <li class="flex items-start gap-2.5 text-xs sm:text-sm text-[#3d3d3a] leading-relaxed">
              <span class="text-[#cc785c] mt-0.5 text-xs">✦</span>
              <span>{highlight}</span>
            </li>
          ))}
        </ul>

        <!-- Skills pills -->
        <div class="mt-6 pt-4 border-t border-[#e6dfd8] flex flex-wrap gap-1.5">
          {item.skills.map((skill) => (
            <span class="px-2.5 py-0.5 rounded text-[11px] font-mono bg-[#faf9f5] text-[#6c6a64] border border-[#e6dfd8]">
              {skill}
            </span>
          ))}
        </div>
      </div>
    ))}
  </div>
</section>
